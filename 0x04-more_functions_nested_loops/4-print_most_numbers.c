#include "main.h"
/**
 *print_most_numbers - prints most numbers
 *Returns: 0
 */

void print_most_numbers(void)
{
	char i;

	for (i >= '0'; i <= '9'; i++)
	{
		if (!(c == '2' || c == '4'))
			_putchar(c)
	}
	_putchar('\n')
}
