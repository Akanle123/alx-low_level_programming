#include "main.h"

/**
 * print_alphabet - Prints the alphabet in lowercase.
 * Return: Alphabet a-z
 */
void print_alphabet(void)
{
	char alphabet;

	for (i = 'a'; i <= 'z'; i++)
		_putchar(alphabet);
	_putchar('\n');
}
